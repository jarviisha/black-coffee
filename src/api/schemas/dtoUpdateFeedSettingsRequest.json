{"properties":{"decay_exponent":{"description":"DecayExponent must be greater than 0. At 0 the recency term is the same\nconstant for every post, which removes recency from the formula rather than\nflattening it — a small exponent is how to ask for a slow decay.","example":1.5,"type":"number"},"fanout_enabled":{"description":"FanoutEnabled is the kill switch for writing prepared timelines. The workers\nstay running and idle, so it is reversible without a restart.","example":true,"type":"boolean"},"fanout_max_followers":{"description":"FanoutMaxFollowers bounds how many followers one event writes to.","example":10000,"type":"integer"},"recency_scale":{"example":20,"type":"number"},"relationship_bonus":{"description":"The local ranking weights:\n\n\tscore = log(1+likes)*10 + recency_scale/(1+hours)^decay_exponent + relationship_bonus\n\nThe three components are meant to sit on a comparable scale, which is what\nthe 0-1000 bounds protect: a relationship_bonus far above the others stops\nranking the feed and merely sorts followed authors to the top.","example":10,"type":"number"},"timeline_enabled":{"description":"TimelineEnabled gates prepared-timeline serving; TimelineRolloutPercent\n(0-100) decides for what share of users. They are separate so a rollout can\nbe paused without losing the percentage to return to.","example":true,"type":"boolean"},"timeline_max_items":{"description":"TimelineMaxItems is entries retained per user timeline, 1-10000. It is a\nper-user memory cost in Redis, not a global one.","example":1000,"type":"integer"},"timeline_refresh_on_miss":{"description":"TimelineRefreshOnMiss lets a miss rebuild that user's timeline inline, on the\nrequest goroutine. Turn it off first when a cold cache is making reads slow.","example":true,"type":"boolean"},"timeline_rollout_percent":{"example":25,"type":"integer"},"timeline_ttl_seconds":{"description":"TimelineTTLSeconds is how long a prepared timeline survives without a write,\n1 second to 90 days.","example":604800,"type":"integer"}},"type":"object","x-readme-ref-name":"dto.UpdateFeedSettingsRequest"}